name: release

# Publishes to PyPI (org secret PYPI_API_TOKEN) + the MCP Registry (GitHub OIDC).
# Trigger: push a version tag, e.g. `git tag v0.1.0 && git push origin v0.1.0`.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}   # manual re-run of the MCP Registry publish without bumping the package version

permissions:
  contents: read
  id-token: write   # required for the MCP Registry OIDC login

jobs:
  build-and-publish-pypi:
    if: github.event_name == 'push'   # only on a version tag; skipped on manual dispatch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tooling
        run: python -m pip install --upgrade build

      - name: Run gates before publishing
        run: |
          python -m pip install -e ".[dev]"
          python -m pytest tests/ -q -k "not smoke"

      - name: Build sdist + wheel
        run: python -m build

      - name: Publish to PyPI (org API token)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}

  # Externalizes volatile runtime config (upstream base URL + citation grammar)
  # as a gzipped release asset. NON-BLOCKING: PyPI never waits on it, clients fall
  # back to the hardcoded default if it is absent. Its URL is built only in code
  # (unlinked), so download_count = a human actually used the connector.
  build-and-upload-runtime-manifest:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Compress + checksum
        run: |
          test -f runtime/tr-runtime.json
          gzip -9 -n -c runtime/tr-runtime.json > tr-runtime.json.gz
          sha256sum tr-runtime.json.gz > tr-runtime.json.gz.sha256

      - name: Upload as release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.ref_name }}"
          gh release create "$TAG" --title "$TAG" \
            --notes "tr-eli-mcp $TAG + runtime manifest (base URL + citation grammar, sha256 alongside)" \
            tr-runtime.json.gz tr-runtime.json.gz.sha256 \
          || gh release upload "$TAG" tr-runtime.json.gz tr-runtime.json.gz.sha256 --clobber

  publish-mcp-registry:
    needs: build-and-publish-pypi
    # run after a successful PyPI publish, OR standalone on manual dispatch
    if: always() && (github.event_name == 'workflow_dispatch' || needs.build-and-publish-pypi.result == 'success')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # OIDC login to the MCP Registry
    steps:
      - uses: actions/checkout@v4

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_amd64.tar.gz" | tar xz mcp-publisher
          sudo mv mcp-publisher /usr/local/bin/

      - name: Login to MCP Registry (GitHub OIDC)
        run: mcp-publisher login github-oidc

      - name: Publish server.json to MCP Registry
        run: mcp-publisher publish
